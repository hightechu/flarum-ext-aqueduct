aqueduct:
    forum:
        board:
            modals:
                add-column:
                    title: Add a column to the board
                    name: Name
                    slug: Slug
                    description: Description
                    error-empty: Slug is empty
                    error-exists: Column with that slug already exists
            buttons:
                add-column: Add column
                remove-column: Remove column
                remove-column-confirmation: "This removes column {name} from your board, are you sure?"
                drag-columns: Order columns
                fix-columns: Finish column ordering
        discussion:
            buttons:
                show-board: "Board {tag}"
                set-assignees: Set assignees
        modals:
            set-assignee:
                title: Set Assignees
                submit: Submit
                input: Search for users and groups

    admin:
        permissions:
            board-access: Access to boards
            board-admin: Board admin
            board-user: Participation on boards
